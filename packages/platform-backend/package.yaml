name: platform-backend
version: "2.0.0"
description: "Backend: Java 8, Jakarta EE 8, WildFly 26.1, Hibernate"
layer: 1
platforms: [backend]

dependencies:
  - core

provides:
  agents:
    - epost-backend-developer
  skills:
    - backend-javaee
    - backend-databases
  commands:
    - backend/cook
    - backend/test
    - backend/debug

files:
  agents/: agents/
  skills/: skills/
  commands/: commands/backend/

settings_strategy: merge
claude_snippet: CLAUDE.snippet.md
