---
description: "Implement backend features with Jakarta EE patterns"
agent: epost-backend-developer
---

Implement $ARGUMENTS using Jakarta EE 8 patterns on WildFly.

## Instructions

1. Understand the feature requirement
2. Create/update JAX-RS resource (REST endpoint)
3. Create/update CDI service (business logic)
4. Create/update JPA entity and DAO
5. Create DTOs with Bean Validation
6. Write unit tests (JUnit 4 + Mockito)
7. Follow package naming: `no.epost.<module>.<layer>`

## Constraints

- Java 8 syntax only (no var, no records, no switch expressions)
- Jakarta EE patterns only (NO Spring Boot)
- WAR packaging for WildFly deployment
