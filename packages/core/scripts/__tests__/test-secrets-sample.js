// Test file for secret scanner validation
const testSecrets = {
  safeApiKey: "your-api-key-here", // Should be ignored (placeholder)
  safePlaceholder: "placeholder", // Should be ignored
  testExample: "example-key" // Should be ignored
};

// Export for testing
module.exports = testSecrets;
