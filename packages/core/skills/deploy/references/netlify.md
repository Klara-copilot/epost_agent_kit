# Netlify Deploy

## Prerequisites
- `npm i -g netlify-cli`
- `netlify login`

## Commands
```bash
netlify deploy --prod  # deploy to production
netlify deploy         # deploy to draft URL
netlify status         # check auth + site info
```

## Auth Check
```bash
netlify status
```
