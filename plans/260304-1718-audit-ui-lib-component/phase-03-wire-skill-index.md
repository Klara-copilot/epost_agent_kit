# Phase 03: Wire to Skill Index + Connections

## Context Links
- Parent plan: [plan.md](./plan.md)
- Phase 01: [phase-01-core-skill-schema.md](./phase-01-core-skill-schema.md)
- Phase 02: [phase-02-platform-checklists.md](./phase-02-platform-checklists.md)
- Skill index: `.claude/skills/skill-index.json`

## Overview
**Date**: 2026-03-04
**Priority**: P2
**Description**: Register skill in package.yaml, verify connections, run epost-kit init to regenerate .claude/.
**Implementation Status**: Pending

## Parallelization Info
- **Execution Batch**: Batch 3
- **Can Run Parallel With**: None
- **Blocked By**: Phase 01, Phase 02
- **Blocks**: None
- **Exclusive Files**:
  - `packages/design-system/package.yaml` (Modify)
- **Shared Read-Only**:
  - `.claude/skills/skill-index.json` (regenerated by init)

## Key Insights

- NEVER edit `.claude/` directly -- it's regenerated by `epost-kit init`
- Skill must be declared in `packages/design-system/package.yaml` under skills section
- After init, verify skill appears in `.claude/skills/skill-index.json` with correct metadata
- Connections: requires [code-review], enhances [web-ui-lib-dev, ios-ui-lib, android-ui-lib]

## Requirements

### Functional
- Skill registered in package.yaml
- After init: skill-index.json has correct entry
- epost-muji agent can discover skill via skill-discovery protocol

### Non-Functional
- Zero manual edits to `.claude/` directory

## Related Code Files

### Modify (EXCLUSIVE)
- `packages/design-system/package.yaml` -- Add skill entry [OWNED]

### Read-Only (shared)
- `.claude/skills/skill-index.json` -- Verify after init

## Implementation Steps

1. Read `packages/design-system/package.yaml`
2. Add `audit-ui-component` to skills list with path `skills/audit-ui-component`
3. Run `epost-kit init` (or equivalent) to regenerate `.claude/`
4. Verify `.claude/skills/audit-ui-component/SKILL.md` exists
5. Verify `.claude/skills/skill-index.json` contains:
   ```json
   {
     "name": "audit-ui-component",
     "description": "Use when auditing or reviewing a UI library component implementation for quality, tokens, patterns, performance, security across web, iOS, or Android",
     "keywords": ["audit", "ui-lib", "component", "review", "muji", "tokens", "quality"],
     "platforms": ["all"],
     "triggers": ["/audit-component", "audit component", "review component"],
     "agent-affinity": ["epost-muji", "epost-reviewer"],
     "connections": {
       "extends": [],
       "requires": ["code-review"],
       "conflicts": [],
       "enhances": ["web-ui-lib-dev", "ios-ui-lib", "android-ui-lib"]
     }
   }
   ```
6. Verify skill-index count is incremented

## Todo List
- [ ] Add skill to packages/design-system/package.yaml
- [ ] Run init to regenerate .claude/
- [ ] Verify skill-index.json entry
- [ ] Smoke test: ask epost-muji to audit a component, confirm skill loads

## Success Criteria
- Skill appears in regenerated .claude/skills/
- skill-index.json count incremented by 1
- No orphan references

## Risk Assessment
**Risks**: package.yaml format mismatch causes init failure
**Mitigation**: Read existing package.yaml first, follow exact same structure as other skill entries

## Security Considerations
- No security surface in registration step
