# Phase 02: Wire into Routing and Index

## Context Links
- Parent plan: [plan.md](./plan.md)
- Depends on: [Phase 01](./phase-01-create-skill.md)
- Reference: `packages/core/skills/skill-discovery/SKILL.md`

## Overview
**Date**: 2026-03-03
**Priority**: P1
**Description**: Register get-started in skill-index, add to routing tables, update CLAUDE.md
**Implementation Status**: Pending

## Key Insights
- skill-index.json is auto-generated by `epost-kit init` — but source package.yaml or manual entry needed
- Hub-context routing table in `hub-context/SKILL.md` needs new intent row
- CLAUDE.md intent maps in both root and project-level need updating
- skill-discovery trigger table needs entry

## Requirements
### Functional
- Skill appears in `skill-index.json` after init
- Hub routing recognizes "get started" intent
- Skill-discovery can find it via keywords
### Non-Functional
- No breaking changes to existing routing

## Architecture
Registration points:
1. `skill-index.json` — auto-generated, but needs source data
2. `hub-context/SKILL.md` — intent routing table
3. `CLAUDE.md` (both levels) — intent map
4. `skill-discovery/SKILL.md` — task type signals table

## Related Code Files
### Modify (EXCLUSIVE)
- `packages/core/skills/hub-context/SKILL.md` - Add "Onboard" intent row [OWNED]
- `packages/core/skills/skill-discovery/SKILL.md` - Add task signal [OWNED]
### Modify (shared — append only)
- `packages/core/CLAUDE.md.hbs` or equivalent template - Add intent row (if templated)
### Read-Only
- `.claude/skills/skill-index.json` - Verify after init

## Implementation Steps

1. **Add intent to hub-context routing table** (`packages/core/skills/hub-context/SKILL.md`):
   ```
   | Onboard | get started, begin, onboard, new to project, what is this | `/get-started` skill |
   ```

2. **Add task signal to skill-discovery** (`packages/core/skills/skill-discovery/SKILL.md`):
   In "1b. Task Type Signals" table:
   ```
   | get started, onboard, begin, new to project | onboarding | get-started |
   ```

3. **Update CLAUDE.md intent maps** (both `/Users/than/Projects/CLAUDE.md` and `/Users/than/Projects/epost_agent_kit/CLAUDE.md`):
   ```
   | Onboard | get started, begin, new to project, what is this | `/get-started` |
   ```

4. **Verify skill-index.json entry** after running `epost-kit init`:
   Expected entry:
   ```json
   {
     "name": "get-started",
     "description": "(ePost) Onboard to a project — scan codebase, read/create docs, suggest updates",
     "keywords": ["onboard", "get-started", "begin", "new-project", "docs"],
     "platforms": ["all"],
     "triggers": ["get started", "how to begin", "new to project"],
     "agent-affinity": ["epost-researcher"],
     "connections": {
       "extends": [],
       "requires": [],
       "conflicts": [],
       "enhances": ["docs-init", "docs-update"]
     },
     "path": "get-started/SKILL.md"
   }
   ```

5. **Run `epost-kit init`** to regenerate `.claude/` with new skill

## Todo List
- [ ] Add routing row to hub-context
- [ ] Add signal to skill-discovery
- [ ] Update CLAUDE.md intent tables (2 files)
- [ ] Run epost-kit init
- [ ] Verify skill appears in generated skill-index.json
- [ ] Test: type "how to get started" and confirm routing

## Success Criteria
- `/get-started` appears in skill-index.json
- Hub routes "get started" intent correctly
- Skill-discovery finds it via onboarding keywords
- No existing routes broken

## Risk Assessment
**Risks**: CLAUDE.md templates may be handlebars — wrong edit target
**Mitigation**: Check if CLAUDE.md is generated from `.hbs` template or direct edit. Edit source.

## Security Considerations
None — routing metadata only.

## Next Steps
After completion:
1. Manual test: invoke `/get-started` in a sample project
2. Verify delegation to `/docs-init` and `/docs-update` works
