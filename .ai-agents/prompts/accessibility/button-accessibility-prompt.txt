@architect

**Task:** Review all buttons in this view controller file and provide accessibility fixes.

**Context:** I'm editing this file and need to ensure all buttons meet WCAG 2.1 AA standards for iOS VoiceOver accessibility.

**Instructions:**
1. Scan this file for all `UIButton` instances (including `@IBOutlet` and programmatically created buttons)
2. For each button, check against `.cursor/rules/a11y-buttons.mdc`:
   - Has `accessibilityLabel` (or visible title text)?
   - Has `.button` trait set?
   - Icon-only buttons have descriptive labels?
   - State changes reflected in traits/value?
   - Disabled buttons have `.notEnabled` trait?
3. Provide patch-style code edits showing:
   - Exact lines to add/modify
   - Complete accessibility property assignments
   - Before/after code snippets

**Output Format:**
- List each button found with its current state
- Show exact Swift code changes needed
- Use unified diff format or clear before/after blocks
- Reference specific rule from a11y-buttons.mdc when applicable

**Example Output:**
```swift
// Button: closeButton (line 45)
// Issue: Missing accessibilityLabel for icon-only button
// Fix:
closeButton.accessibilityLabel = "Close"
closeButton.accessibilityTraits = .button
```

